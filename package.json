{
  "name": "bus-booking",
  "version": "1.0.0",
   "privayte": true,
  "description": "",
  "scripts": {
    "build": "pnpm -r --workspace-root=false run build",
"dev": "pnpm -r --parallel --stream --workspace-root=false run dev",
    "lint": "pnpm -r --workspace-root=false run lint",
    "format": "pnpm -r --workspace-root=false run format",
    "test": "pnpm -r --workspace-root=false run test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.33.2",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^25.6.0",
    "@typescript-eslint/eslint-plugin": "^8.59.1",
    "@typescript-eslint/parser": "^8.59.1",
    "eslint": "^10.3.0",
    "prettier": "^3.8.3",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  }
}
